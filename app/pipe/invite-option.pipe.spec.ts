import { InviteOptionPipe } from './invite-option.pipe';

describe('InviteOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new InviteOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
